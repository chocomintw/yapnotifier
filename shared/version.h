/* Single source of truth for the version. Included by C++ and by YapNotifier.rc
 * (so no #pragma once / C++ constructs). Keep ts3plugin/package.ini in step; CI checks it. */
#ifndef YAP_VERSION_H
#define YAP_VERSION_H

#define YAP_VERSION     "0.1.0"
#define YAP_VERSION_NUM 0,1,0,0

#define YAP_GITHUB_REPO "chocomintw/yapnotifier"

#endif
